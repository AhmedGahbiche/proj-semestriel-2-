import { ActionPage } from "@/components/ui/action-page";

export default function ResetTriggeredPage() {
  return <ActionPage title="Reset Triggered" description="Triggered traps were reset and synchronized across the fleet." />;
}
