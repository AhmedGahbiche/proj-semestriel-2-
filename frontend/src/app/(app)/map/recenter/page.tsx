import { ActionPage } from "@/components/ui/action-page";

export default function RecenterPage() {
  return <ActionPage title="Map Recenter" description="Map camera re-centered to your current operational zone." />;
}
