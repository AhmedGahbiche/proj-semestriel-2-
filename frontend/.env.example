# Backend connection used by Next.js server routes/components
# Keep this as http://localhost:4000 for local backend dev
BACKEND_URL=http://localhost:4000

# Server-only admin API key used to call the backend.
# For local dev you can set this to the same value as backend ADMIN_BOOTSTRAP_KEY.
BACKEND_ADMIN_API_KEY=change-me-dev-only
